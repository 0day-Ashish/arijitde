import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Distribution Strategy Booking - Schedule Call",
  description: "Book a strategic consultation call or distribution slot with Arijit De's wealth management support team.",
};

export default function BookLayout({ children }: { children: React.ReactNode }) {
  return children;
}
